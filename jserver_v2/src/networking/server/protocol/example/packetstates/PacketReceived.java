package networking.server.protocol.example.packetstates;

import networking.server.protocol.PacketState;

public class PacketReceived implements PacketState
{
	public static PacketReceived packetReceived = new PacketReceived();
}
